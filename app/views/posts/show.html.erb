

<div class="row">
  <div class="span3">
	      <%= link_to 'Home', start_path, :class => 'btn btn-mini' %> <span class="divider">/</span></li>
		  <%= link_to 'New Discussion Item', new_post_path(:item_type => 'Discussion'), :class => 'btn btn-mini' %>
  </div>
</div>

<%= render 'show' %>

